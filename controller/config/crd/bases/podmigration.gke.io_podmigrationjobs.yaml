---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: podmigrationjobs.podmigration.gke.io
spec:
  group: podmigration.gke.io
  names:
    kind: PodMigrationJob
    listKind: PodMigrationJobList
    plural: podmigrationjobs
    singular: podmigrationjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodMigrationJob tracks the execution of a single Pod Migration
          event.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PodMigrationJobSpec defines the execution targets.
            properties:
              podRef:
                description: PodRef references the pod to migrate.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - podRef
            type: object
          status:
            description: PodMigrationJobStatus defines the observed state.
            properties:
              phase:
                description: PodMigrationJobPhase defines the current state in the
                  lifecycle.
                type: string
              pvsToDetach:
                description: PVsToDetach lists the Persistent Volume names we are
                  waiting to detach.
                items:
                  type: string
                type: array
              snapshotRef:
                description: SnapshotRef references GKE's native PodSnapshot name.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
