apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
    components.gke.io/layer: addon
  labels:
    addonmanager.kubernetes.io/mode: Reconcile
  name: podsnapshotstorageconfigs.podsnapshot.gke.io
spec:
  group: podsnapshot.gke.io
  names:
    kind: PodSnapshotStorageConfig
    listKind: PodSnapshotStorageConfigList
    plural: podsnapshotstorageconfigs
    shortNames:
    - pssc
    singular: podsnapshotstorageconfig
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PodSnapshotStorageConfig is the general storage configuration
          for customers to define how snapshots are stored and managed.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PodSnapshotStorageConfigSpec defines the desired state of
              PodSnapshotStorageConfig.
            properties:
              snapshotStorageConfig:
                description: SnapshotStorageConfig defines the configuration for the
                  snapshot's underlying storage.
                properties:
                  gcs:
                    description: GCS contains the configuration for the GCS setups
                    properties:
                      bucket:
                        description: |-
                          Bucket is the bucket name going to be used for the storing pod snapshots.
                          Example: "bucket-for-abc"
                        pattern: ^[a-z0-9._-]+$
                        type: string
                      path:
                        description: |-
                          Path is the path inside the bucket defined in the above field that
                          indicates a child directory that going to be used for storing pod
                          snapshots.
                          Example: "a/b/c", then the stored path for pod snapshots will be
                          "gs://bucket-for-abc/a/b/c"
                        type: string
                      tokenSource:
                        default: podKSA
                        description: |-
                          TokenSource determines the authentication strategy for GCS operations.
                          "podKSA" (default) utilizes the Pod's own Kubernetes Service Account (Workload Identity).
                          "federatedP4SA" utilizes the central GKE Project Service Agent to mint path-restricted, downscoped tokens on-demand.
                        enum:
                        - podKSA
                        - federatedP4SA
                        type: string
                    required:
                    - bucket
                    type: object
                type: object
            required:
            - snapshotStorageConfig
            type: object
          status:
            description: PodSnapshotStorageConfigStatus defines the observed state
              of PodSnapshotStorageConfig.
            properties:
              conditions:
                description: Represents the latest available observations of a PodSnapshotStorageConfig's
                  current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration if set, represents the .metadata.generation
                  that the PodSnapshotStorageConfig condition was set based upon.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
