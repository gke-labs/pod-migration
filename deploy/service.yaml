apiVersion: v1
kind: Service
metadata:
  name: pod-migration-service
  namespace: default
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    app: pod-migration-controller
