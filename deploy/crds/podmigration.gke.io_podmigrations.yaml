---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: podmigrations.podmigration.gke.io
spec:
  group: podmigration.gke.io
  names:
    kind: PodMigration
    listKind: PodMigrationList
    plural: podmigrations
    singular: podmigration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodMigration is the Schema for the podmigrations configuration
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PodMigrationSpec defines the desired config.
            properties:
              storage:
                description: StorageSpec defines the snapshot storage target configuration.
                properties:
                  folderManagement:
                    description: FolderManagement specifies how the target snapshot
                      folders are managed.
                    type: string
                  location:
                    description: Location is the storage location URI (e.g. gs://bucket-name/prefix-path)
                    type: string
                required:
                - location
                type: object
            required:
            - storage
            type: object
          status:
            description: PodMigrationStatus defines the observed state.
            properties:
              active:
                description: Active determines if GKE storage config and policy have
                  been generated.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
