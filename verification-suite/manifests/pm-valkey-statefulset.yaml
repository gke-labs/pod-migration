apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pm-valkey
  namespace: default
spec:
  serviceName: pm-valkey-service
  replicas: 1
  selector:
    matchLabels:
      app: pm-valkey
  template:
    metadata:
      labels:
        app: pm-valkey
        live-migration.gke.io/enabled: "true"
        pod-migration.gke.io/enabled: "true"
        podsnapshot.gke.io/enabled: "true"
      annotations:
        live-migration.gke.io/enabled: "true"
        pod-migration.gke.io/enabled: "true"
    spec:
      serviceAccountName: pm-test-ksa
      runtimeClassName: gvisor
      nodeSelector:
        sandbox.gke.io/runtime: gvisor
      tolerations:
        - key: sandbox.gke.io/runtime
          operator: Equal
          value: gvisor
          effect: NoSchedule
      containers:
        - name: valkey
          image: valkey/valkey:8-alpine
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 6379 }]
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { memory: 512Mi }
          readinessProbe:
            tcpSocket: { port: 6379 }
            periodSeconds: 2
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: pm-valkey-service
  namespace: default
spec:
  selector:
    app: pm-valkey
  ports:
    - port: 6379
      targetPort: 6379
